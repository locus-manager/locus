# Migration `20200620184046-init`

This migration has been generated by Ricardo Mello at 6/20/2020, 6:40:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."User" (
"email" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  ,"phone" TEXT NOT NULL  )

CREATE TABLE "quaint"."Place" (
"code" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  )

CREATE TABLE "quaint"."UsersOnPlaces" (
"checkInDate" DATE  DEFAULT CURRENT_TIMESTAMP ,"checkoutDate" DATE   ,"placeId" INTEGER NOT NULL  ,"userId" INTEGER NOT NULL  ,
    PRIMARY KEY ("userId","placeId"),FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("placeId") REFERENCES "Place"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "quaint"."Place.code" ON "Place"("code")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200620184046-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:./dev.db"
+}
+
+model User {
+  id    Int     @default(autoincrement()) @id
+  email String  @unique
+  name  String
+  phone String
+}
+
+model Place {
+  id     Int     @default(autoincrement()) @id
+  name   String
+  code   String  @unique
+}
+
+model UsersOnPlaces {
+  user          User       @relation(fields: [userId], references: [id])
+  userId        Int        // relation scalar field (used in the `@relation` attribute above)
+  place         Place      @relation(fields: [placeId], references: [id])
+  placeId       Int        // relation scalar field (used in the `@relation` attribute above)
+  checkInDate   DateTime?  @default(now())
+  checkoutDate  DateTime?
+
+  @@id([userId, placeId])
+}
```


