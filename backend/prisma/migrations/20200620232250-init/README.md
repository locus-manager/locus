# Migration `20200620232250-init`

This migration has been generated by Ricardo Mello at 6/20/2020, 11:22:50 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."User" (
"email" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  ,"phone" TEXT NOT NULL  )

CREATE TABLE "quaint"."Place" (
"code" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  )

CREATE TABLE "quaint"."UsersOnPlaces" (
"checkinDate" DATE  DEFAULT CURRENT_TIMESTAMP ,"checkoutDate" DATE   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"placeId" INTEGER NOT NULL  ,"userId" INTEGER NOT NULL  ,FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("placeId") REFERENCES "Place"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "quaint"."Place.code" ON "Place"("code")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200620232250-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,31 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:./dev.db"
+}
+
+model User {
+  id    Int     @default(autoincrement()) @id
+  email String  @unique
+  name  String
+  phone String
+}
+
+model Place {
+  id     Int     @default(autoincrement()) @id
+  name   String
+  code   String  @unique
+}
+
+model UsersOnPlaces {
+  id            Int        @default(autoincrement()) @id
+  user          User       @relation(fields: [userId], references: [id])
+  userId        Int        // relation scalar field (used in the `@relation` attribute above)
+  place         Place      @relation(fields: [placeId], references: [id])
+  placeId       Int        // relation scalar field (used in the `@relation` attribute above)
+  checkinDate   DateTime?  @default(now())
+  checkoutDate  DateTime?
+}
```
