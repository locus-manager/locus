# Migration `20200620190445-init`

This migration has been generated by Ricardo Mello at 6/20/2020, 7:04:45 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_UsersOnPlaces" (
"checkInDate" DATE  DEFAULT CURRENT_TIMESTAMP ,"checkoutDate" DATE   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"placeId" INTEGER NOT NULL  ,"userId" INTEGER NOT NULL  ,FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("placeId") REFERENCES "Place"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_UsersOnPlaces" ("checkInDate", "checkoutDate", "placeId", "userId") SELECT "checkInDate", "checkoutDate", "placeId", "userId" FROM "quaint"."UsersOnPlaces"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."UsersOnPlaces";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_UsersOnPlaces" RENAME TO "UsersOnPlaces";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200620184046-init..20200620190445-init
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:./dev.db"
 }
 model User {
   id    Int     @default(autoincrement()) @id
@@ -20,13 +20,12 @@
   code   String  @unique
 }
 model UsersOnPlaces {
+  id            Int        @default(autoincrement()) @id
   user          User       @relation(fields: [userId], references: [id])
   userId        Int        // relation scalar field (used in the `@relation` attribute above)
   place         Place      @relation(fields: [placeId], references: [id])
   placeId       Int        // relation scalar field (used in the `@relation` attribute above)
   checkInDate   DateTime?  @default(now())
   checkoutDate  DateTime?
-
-  @@id([userId, placeId])
 }
```


